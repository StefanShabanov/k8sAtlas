# Cloudflare API Token Secret - EXAMPLE
#
# This is a template for the Cloudflare API token secret.
# DO NOT commit the actual secret to git!
#
# To create the secret, use:
#   kubectl create secret generic cloudflare-api-token \
#     --from-literal=api-token=YOUR_CLOUDFLARE_API_TOKEN \
#     --namespace cert-manager
#
# Or copy this file to cloudflare-api-token-secret.yaml,
# replace YOUR_CLOUDFLARE_API_TOKEN_HERE with your actual token,
# and apply it:
#   kubectl apply -f cloudflare-api-token-secret.yaml
#
# How to get Cloudflare API token:
# 1. Go to: https://dash.cloudflare.com/profile/api-tokens
# 2. Click "Create Token"
# 3. Use template: "Edit zone DNS"
# 4. Permissions: Zone.DNS:Edit
# 5. Zone Resources: Include - Specific zone - yourdomain.com
# 6. TTL: Set as needed or leave default
# 7. Click "Continue to summary" then "Create Token"
# 8. Copy the token (you won't see it again!)
#
# IMPORTANT: Add cloudflare-api-token-secret.yaml to .gitignore!

---
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: cert-manager
type: Opaque
stringData:
  api-token: "YOUR_CLOUDFLARE_API_TOKEN_HERE"
