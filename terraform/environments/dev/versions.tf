# terraform and provider version constraints
#
# this file defines the required terraform version and provider versions
# pinning versions ensures consistent behavior across different environments

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 6.0"
    }
    google-beta = {
      source  = "hashicorp/google-beta"
      version = "~> 6.0"
    }
  }
}

# default provider configuration
# individual resources can override these settings if needed
provider "google" {
  project = var.project_id
  region  = var.region
}

provider "google-beta" {
  project = var.project_id
  region  = var.region
}
